import MenuWrapper from './components/MenuWrapper';
export const metadata = {
  title: 'Menu',
};

const page = () => {
  return (
    <main>
      <MenuWrapper />;
    </main>
  );
};

export default page;
